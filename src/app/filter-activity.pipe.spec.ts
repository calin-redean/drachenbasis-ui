import { FilterActivityPipe } from './filter-activity.pipe';

describe('FilterActivityPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterActivityPipe();
    expect(pipe).toBeTruthy();
  });
});
