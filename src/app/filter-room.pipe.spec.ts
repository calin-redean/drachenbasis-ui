import { FilterRoomPipe } from './filter-room.pipe';

describe('FilterRoomPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRoomPipe();
    expect(pipe).toBeTruthy();
  });
});
